{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissões base do app Vorlo (janela principal).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
